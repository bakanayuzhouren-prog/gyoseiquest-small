# 次の画像プロンプト（てらしぃ／Codex）

民法記述は **Q53まで PNG・配線済**。行政法記述は **Q16まで PNG・配線済**（Q1-2／Q2-2 含む）。

次: `codex-batch-gyosei-kijutsu-q17-q35.md`（Q17〜Q35・**19問**）。

行政法正本は Q35 が最後。20問枠に対し1問足りないが、足りない分の新Qは作らない。この19問を一気に出す。

各ファイル内の ` ```text ` を1問ずつ生成。

**上書き禁止**

- 民法 `minpou-kijutsu/q1.png`〜`q53.png`（`q1-1` `q11-2` 含む）
- 行政法 `gyosei-kijutsu/q1.png`〜`q16.png`（`q1-2` `q2-2` 含む）

生成後X禁止。誤情報チェックは `_image-legal-check.md`。
