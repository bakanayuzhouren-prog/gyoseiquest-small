# AGENTS.md

このファイルは、このリポジトリで作業するAIエージェント向けの共通ルールです。作業前に必ず読み、ここに書かれた方針を優先してください。

## エージェント組織

このリポジトリでは、作業を次の3役で進めます。

- スーパーバイザー: てらしぃの指示を受け、目的・制約・優先順位を整理し、オペレーターに渡す適切な作業プロンプトと実施方針を考案する。
- オペレーター: スーパーバイザーの指示を受け、コード・コンテンツ・設定ファイルの作成や修正を実施する。
- チェッカー: オペレーターが作成したコードをWeb上または実行環境で動かし、不具合、表示崩れ、明らかな要件違反がないか確認する。

## 作業フロー

1. てらしぃが要望・修正内容・確認したいことを伝える。
2. スーパーバイザーが依頼内容を分解し、実施範囲、注意点、検証観点を明確にする。
3. オペレーターがスーパーバイザーの方針に従って作業する。
4. チェッカーが変更後の画面・挙動・ログ・Lintなどを確認する。
5. 明らかにおかしな動作、不具合、要件から外れた実装がある場合、チェッカーはスーパーバイザーへ報告する。
6. スーパーバイザーは報告内容をもとに修正方針を再設計し、オペレーターへ再指示する。
7. オペレーターが修正し、チェッカーが再確認する。このループを、明確な不具合が解消するまで繰り返す。
8. 可否判断が不明な場合、仕様・見た目・学習内容の正確性などを推測で決めず、てらしぃへ報告して判断を仰ぐ。

## 役割別ルール

### スーパーバイザー

- てらしぃの指示を最優先し、目的を短く明文化する。
- 作業範囲が広い場合は、実装対象・触らない対象・検証方法を切り分ける。
- オペレーターに渡す指示は、対象ファイル、期待する挙動、禁止事項、確認方法がわかる形にする。
- チェッカーから不具合報告を受けたら、原因仮説と修正方針を整理して再指示する。
- 判断が分かれるデザイン、文言、学習内容、仕様変更は、てらしぃに確認する。
- 教材画像を生成・追加した場合は、生成で終わらせず、アプリ内の配置方針まで決める。
  - GPT Image等で教材画像、図解、アバターを作る場合は、先に `skills/gyosei-image-style/SKILL.md` を読み、デザイン、アバター、配置方針を統一する。
  - 画像ごとに、関連する問題、選択肢、見て聞いて覚えるカード、チャンク候補を洗い出す。
  - 直接対応する問題・選択肢がある画像は、「もっと深掘る」に自動またはタグで配置するようオペレーターへ指示する。
  - 直接対応する問題がない横断整理画像は、近い単元のチャンク、補助資料、または学習カードとして表示できる導線を用意する。
  - 他科目でも同じく、画像の論点名、条文番号、制度名、比較軸から配置先を推定し、推定根拠をチェッカーが確認できる形で残す。
  - 配置判断が教材内容上あいまいな場合は、勝手に埋め込まず、候補と理由をてらしぃに報告する。

### オペレーター

- スーパーバイザーの指示に沿って、必要最小限のファイルを変更する。
- 既存の未コミット変更を壊さず、無関係な整形やリファクタを混ぜない。
- 実装前に周辺コードを読み、既存パターンに合わせる。
- 変更後は、チェッカーが検証しやすいように変更点と確認ポイントを整理する。
- 仕様が曖昧な場合は勝手に大きく決めず、スーパーバイザーへ戻す。
- てらしぃが添付画像をもとに教材画像・図解・表を作るよう依頼した場合は、まず添付画像の構造を優先する。
  - 元画像の列名、行の並び、比較軸、強調語句、注釈の意味を読み取り、勝手に別構成へ作り替えない。
  - 表が中心の資料では、表の読みやすさを主役にし、イラストは理解を助ける補助として添える。
  - イラストは、人物、吹き出し、矢印、アイコンなどの簡単なものを使い、条文・要件・効果の関係が直感的にわかる位置へ置く。
  - イラストや装飾が本文、表、重要語句と重ならないよう、最初から余白を確保して配置する。
  - 文字量が多い場合は、無理に漫画化せず、表・短い補足・小さなイラストの順で整理する。
  - 元画像から抽出した知識を要約する場合でも、列名を変えたら中身も必ず列名に合わせて書き直す。
- 教材図は、正確な情報を箱へ並べただけの「資料画面」で終わらせず、学習者が続きを見たくなる小さな物語として設計する。
  - 手続は旅・チェックポイント、比較は対決・分岐、優先順位はレース、要件は関門、効果は救済ルートなど、論点に合う視覚メタファーを一つ選ぶ。
  - 各図に、中心となる判断軸、ひっかけカード、短い暗記フレーズを原則一つずつ置く。
  - 人物・道具・矢印・表情を使って「誰が、何を、どう動かすか」を見せる。ただし装飾のためだけの人物は増やさない。
  - 共通ガイド役として、緑色のフクロウ講師を余白へ配置できる。本文や矢印を隠さず、指し示す対象を明確にする。
  - 背景は温かいオフホワイト、主要色は紺・青・ティール・緑、注意はアンバー、誤りだけ赤とし、科目をまたいでも同じ意味で使う。
  - 大見出し、図解、判断軸、ひっかけ、暗記の芯の順に視線が流れる構成を優先する。
  - 既存のそっけない図を刷新するときも、条文番号、比較軸、結論、既存参照キーを保持し、見た目だけで法律上の意味を変えない。
  - 一括置換前に代表図を目視し、承認済みの方向と文字密度が一致してから同系統へ展開する。

### チェッカー

- オペレーターの変更をWeb上または実行環境で確認する。
- UI変更では、対象画面を開き、表示崩れ、遷移、操作、エラーの有無を確認する。
- UI、画像表示、画面遷移、ボタン表示条件、学習導線を変更した場合は、原則として `npm run web` または既存のExpo開発サーバーでWeb画面を開き、実際の操作で確認する。
  - 該当画面まで遷移し、変更したボタン、リンク、モード、画像、チャンク表示、もっと深掘る表示が期待どおり出るか確認する。
  - PC幅だけでなく、可能な範囲でスマホ相当の狭い幅でも確認し、文字切れ、重なり、横スクロール、押せないボタンがないか見る。
  - Web確認が環境都合でできない場合は、確認できなかった理由、代替で実施した確認、残るリスクをスーパーバイザーとてらしぃへ報告する。
- Web確認で許可が必要になりやすい操作は、次の標準手順として扱う。
  - Expo Web起動は、まず `npm run web` または `npx expo start --web --port <空きポート>` を試す。
  - PowerShellで `npm.ps1` が拒否される場合は、`npm.cmd` を使って起動する。
  - 開発サーバー起動がネットワーク制限やサンドボックスで失敗した場合は、同じ目的のコマンドを権限付きで再実行する。
  - ブラウザ操作は、利用可能ならBrowserプラグインを使う。利用できない場合は、Playwrightで実際のWeb画面を開いて確認する。
  - Playwrightなどのブラウザ起動で権限確認が必要になった場合は、Web画面確認のための標準チェッカー作業として許可を求める。
  - 一度てらしぃが同種のWeb確認を許可した場合、その許可範囲で、ログイン、対象画面への遷移、PC幅確認、スマホ幅確認、スクリーンショット保存、Lint再実行まで自律的に進める。
  - ただし、OSやCodex側の安全機構が要求する承認そのものは、AGENTS.mdでは省略できない。承認が必要な画面が出た場合は、目的を短く説明して許可を求める。
- 教材画像・図解・表を変更した場合は、生成後のPNG/SVGを必ず目視確認し、次の項目を確認する。
  - 文字、吹き出し、人物、矢印、表の罫線が重なっていないこと。
  - 文字が枠からはみ出していないこと、途中で不自然に改行されていないこと。
  - てらしぃが提示した元資料の構造、列名、重要語句、学習上の意図から大きく外れていないこと。
  - 表は、列の意味と中身が対応していること。列名を変更した場合は、各行の内容も必ず合わせて直すこと。
  - 法律知識の正確性に疑いがある場合は、合格・不合格を断定せず、スーパーバイザーへ確認事項として戻すこと。
- 画像チェックでは、ファイル生成コマンドの成功だけで合格にしない。完成画像を開き、少なくとも対象画像1枚を実際に見てから判断する。
- 画面チェックでは、PC幅だけでなく、可能な範囲でスマホ相当の狭い幅でも、文字切れ、ボタン重なり、スクロール不能がないか確認する。
- ロジック変更では、関連コマンド、Lint、簡易動作確認、ログ確認を行う。
- 明らかな不具合は、再現手順、期待結果、実際の結果をスーパーバイザーへ報告する。
- 可否判断が主観的・仕様依存・教材内容依存の場合は、合否を断定せず、てらしぃ確認事項として報告する。

## プロジェクト概要

- `gyosei-quest-small` は Expo / React Native / Expo Router ベースの学習アプリです。
- 行政書士試験向けのクイズ、学習テキスト、判例・条文・深掘り解説、画像付き教材を扱います。
- UIコードと教材データが同じリポジトリにあるため、コード変更とコンテンツ変更を混ぜないように注意してください。

## 主要ディレクトリ

- `app/`: Expo Router の画面・ルーティング。
- `components/`: 画面横断のReactコンポーネント。
- `src/`: クイズ、学習データ、画像マップ、生成済みデータ、アプリ内部ロジック。
- `utils/`: クイズ処理、学習支援、検索、TTS、表示補助などの共有ユーティリティ。
- `data/`: Markdown教材、ナレッジ、ピン留めデータ、CSVなどのコンテンツ。
- `content/`: 教科書系コンテンツの原稿・ドラフト。
- `assets/`: アプリで使用する画像・音声などの静的アセット。
- `scripts/`: Google Sheets同期、教材生成、画像生成、変換、検証などの保守スクリプト。
- `docs/`: セットアップや運用メモ。
- `tmp/`, `temp_images/`, `logs/`, `scripts/output/`: 作業用・生成物・ログ置き場。必要な場合だけ触る。

## 開発コマンド

- 依存関係のインストール: `npm install`
- 開発サーバー: `npm run dev` または `npm start`
- Web起動: `npm run web`
- Lint: `npm run lint`
- クイズデータ変換: `npm run build:questions`
- クイズ同期: `npm run sync:questions`
- 学習データ同期: `npm run sync:learn`
- ナレッジMarkdown出力: `npm run export:knowledge-md`
- チャット用Markdown index生成: `npm run build:chat-md-index`
- 全同期: `npm run sync:all`

Google Sheets連携や外部APIを使うスクリプトは `.env` が必要です。秘密情報をコミットしないでください。

## コード規約

- TypeScript / TSX は既存の書き方に合わせ、`strict: true` 前提で型を崩さないでください。
- パスエイリアス `@/*` はリポジトリルート基準です。既存コードのimport方針に合わせてください。
- 画面は Expo Router のファイルベースルーティングに従って `app/` に配置してください。
- 共有UIは `components/`、純粋な変換・判定・整形処理は `utils/` に置くのを基本にしてください。
- 既存のテーマ、色、余白、コンポーネント構造を優先し、大きなデザイン変更は依頼範囲内に留めてください。
- コメントは、複雑な変換・同期・教材生成の意図を残す場合にだけ短く書いてください。

## コンテンツとデータの扱い

- `src/questions.js`, `src/learn.js`, `src/*Images*`, `data/knowledge/**` などは大きな生成・同期データを含みます。手編集する前に生成元や同期スクリプトを確認してください。
- 教材本文、判例名、条文番号、正誤、選択肢、解説は学習成果に直結します。推測で修正せず、根拠を確認してください。
- 画像パスやIDはクイズ・深掘り・学習ページと連動していることがあります。リネームや移動は参照元を検索してから行ってください。
- Markdown教材では既存の見出し、タグ、リンク、画像記法に合わせてください。
- 自動生成スクリプトを実行した場合は、生成差分が依頼範囲に収まっているか必ず確認してください。

## 記述スコープ

択一問題の肢を記述問題に変換して解く機能。正本・問題文の作り方はてらしぃ確定仕様に従う。

### 機能概要

- 択一問題画面の「記述スコープ」チップは常時表示（記述科目以外）。設定スイッチで消さない。
- チップON → 肢をタップ → その肢の論点を記述問題に変換 → 入力して回答 → 結果画面は記述式と同じ採点UI（`isDescriptiveScope`）。

### 問題文の作り方（最重要）

- ざっくり「〜について述べよ」だけでは不可。
- 模範解答で書いてほしい論点を、問題文側で①②などにより誘導する（行政書士試験の記述に近い）。
- 例: 「①成年後見人を付すことが必要か、②後見監督人を同時に選任することが必要か、をそれぞれ明らかにして記述しなさい。」
- 模範解答は、その誘導に対応する結論の文（複数論点なら接続詞でつなぐ）。
- 「40字」に無理に縛らない。誘導に答えられる長さでよい。

### データ配置

- 正本: `src/descriptiveScopeBank.ts` の `DESCRIPTIVE_SCOPE_BY_QUESTION_CHOICE`
- キー: 問題文ハッシュ（`getQuestionTextHash`）→ 肢index（0始まり）の配列
- アプリはバンク優先。無い肢だけAI/フォールバック
- 元の択一問題文・肢の全文転載で記述問題を作らない（著作権・学習効果）

### 品質

- 正誤の結論は既存の正解肢・解説・ナレッジと矛盾させない
- `needs_review` な論点は推測で断定しない

## もっと深掘る表示・解説文のルール

- 「もっと深掘る」や肢別解説では、Markdown記法や独自タグがそのまま見えない状態を完了条件にしてください。特に `[[red:...]]`、`**太字**`、Markdown表、番号付き見出しがカードタイトルに吸い上げられる箇所を確認してください。
- 見出しと本文は必ず分けてください。`■ 本肢の正誤`、`■ 直す場所`、`■ つまり`、`■ 暗記` などの見出しの直後には空行を入れ、本文を次の段落として読ませてください。
- 解説は、今回の問題を解くために必要な誤りの原因を先に示し、周辺知識は短く補足してください。長い比較表や「パターンA/B」のように、この問題との関係が直感的に分からない表現は避けてください。
- 文章が長くなる場合は、1段落1論点に分け、先に結論、次に理由、最後に暗記フレーズの順にしてください。1つの段落に複数論点を詰め込まないでください。
- 誤りの決め手、正しい語句、ひっかけ語は `[[red:...]]` で赤字化してください。ただし、赤字タグをカードタイトルだけに置く構成は避け、本文行でも意味が伝わるようにしてください。
- 表を使う場合は、何を比較している表かを見出しで明示し、列名は「誤りの言い方」「正しい言い方」など問題文との関係が分かる名前にしてください。説明不能な汎用比較表は入れないでください。
- 既存解説を直すときは、単に情報量を増やすのではなく、「なぜ不正解か」が一読で分かるように短く再構成してください。

## 判例4コマ（もっと深掘る）

てらしぃが判例の流れを「もっと深掘る」に入れるよう指示した場合、または判例カードを新規追加・刷新する場合は、次を標準とする。

### 目的

- 初学者が「何が起きて、何が争われて、何が論点で、どう判決されたか」を一読で追えるようにする。
- 条文番号や結論だけ並べず、物語として4コマで見せる。

### 必須構成（4コマ）

1. **まず何が起きた** … 事実の入口（誰が・何をして・何が起きた）
2. **当事者の言い分** … 訴え側／相手側の主張を短く
3. **裁判所が見た争点** … 試験で問われる判断軸（私人間効力、処分性、裁量など）
4. **判決の着地** … 結論と、試験で切るひっかけ一言

見出しは `**1. …**` 形式。各番号の直前は改行。見出し直後は空行を入れて本文を次段落にする。

### 口調

- 先生と生徒ではなく、**友人同士のフランクな喋り**で書く。
- 「〜だよね」「要するに」「ここが試験の芯」など、会話調でよい。
- ただしスラング過多・下品・当事者への嘲笑は避ける。法律用語（私人間効力、裁量、学則など）は残し、その直後に一言で噛み砕く。

### 根拠と禁止

- 公開されている判例情報（裁判所サイト、定評ある解説、Wikipedia等の概要）と、アプリ内の既存カード・教科書・ナレッジを突き合わせて書く。
- 問題文・解説の全文転載はしない。事実は要約し、人物名・数値・エピソードは必要最小限にする。
- 確認できない細部は推測で断定しない。未確認なら `needs_review` か「要確認」として残す。

### 配置

- 見て聞いて覚えるカードの `deepdive`（もっと深掘る）に入れる。
- 4コマの前後に、短い **結論** と **試験で切るポイント／暗記** を置く。
- 既存の結論カードがある場合は、結論を消さず4コマを足す。
- 画像付き4コマ（`[[comic4:]]`）がある場合は画像と矛盾しない本文にする。画像が無いときは文章4コマだけで完了してよい。

### 完了条件

- 対象判例カードの `LEARN_DEEPDIVE`（または生成元の `deepdive`）に4コマが入っている。
- アプリで「もっと深掘る」を開き、番号見出しがカードタイトルに吸い上げられず、本文として読める。
- ひっかけ語は本文でも `[[red:...]]` で分かる。

## 教材追加の標準完了条件

- てらしぃが「この知識を追加」「この問題を実装」「見て聞いて覚えるに追加」などと指示した場合は、カード本文の追加だけで完了にしないでください。
- 原則として、該当知識を `見て聞いて覚えるモード` の本文と `もっと深掘る` の解説・具体例・図解までセットで実装してください。
- 問題演習化が自然な論点は、可能な範囲で `ボーナスステージ` にも、元資料の文章を転載しない自作問題として追加してください。
- 実装後は、`src/learnExports.js` 等の実際にアプリが読む結合経路に乗っているか確認し、対象カードの `LEARN_CONTENT` と `LEARN_DEEPDIVE` が同じ index で空でないことを確認してください。
- `見て聞いて覚えるモードぷらす` に追加するカードは、データに入っただけで完了にしないでください。ぷらす画面の表示フィルタまで通るか確認し、`LEARN_SOURCE` またはカード本文に `TAC` / `模試` / `moshi` 等のぷらす抽出キーワードが含まれる状態にしてください。
- 追加後は、対象科目について `LEARN_CONTENT`、`LEARN_DEEPDIVE`、`LEARN_SOURCE` を同じ index で確認し、ぷらす画面相当の抽出条件で件数が増えていることを node 等で確認してください。例: `source + text` を `/TAC|模試|moshi/i` で判定し、追加カードが抽出されるかを見る。
- てらしぃが「問題を生成」「見て聞いて解くモードの問題を増やす」「TAC2ほぼ全問」などと指示した場合は、見て聞いて覚えるカードだけでなく、`src/tac_bonus_questions.js` のボーナス問題も同時に作成してください。
- 模試由来の問題生成では、通常の5肢択一だけでなく、論点に応じて `多肢選択` の穴埋め形式、`記述` の40字答案選択形式も作成候補にしてください。アプリUIが直接自由記述採点に対応していない場合は、既存UIに乗る答案選択型で実装してください。
- ボーナス問題を追加した場合は、`src/bonus_questions.js` 経由で `BONUS_QUESTIONS` に結合されること、対象科目・分野の件数が増えること、各問題に `choices`、`answer`、`explain`、`isBonus: true` があることを確認してください。多肢選択型は `choices` が空だと問題フィルタから落ちるため、`slots` と `wordBank` に加えて案内用の `choices` を最低1件入れてください。
- もっと深掘るの内容には、定義だけでなく、てらしぃが次に同種問題を解けるように、具体例、比較表、ひっかけ語、短い復習チャンクのいずれかを必ず入れてください。
- 法律知識の正確性に不安がある場合は、公式資料や既存教材で確認し、確認できない部分は推測で断定せず、未確認事項として報告してください。

## 類似問題・科目横断チャンク

- 問題や解説を追加・修正するときは、全科目の問題・学習カード・もっと深掘るから、同じ条文、判例、要件、効果、判断基準、似た言い回しを検索してください。
- 同じ科目内に類似問題がある場合は、関連問題チャンクを積極的に表示し、問題番号・肢番号・共通点・相違点・本番での見分け方を明記してください。
- 憲法と行政法、民法と商法など、科目をまたいで混同しやすい論点も、科目横断チャンクとして表示してください。単に関連語を並べず、「どの場面で」「誰に」「何が必要か」「例外は何か」を比較できる内容にしてください。
- 関連問題チャンクは、現在学習している問題を置き換えたり削除したりせず、現在の問題を関連問題群の一つとして必ず残してください。
- 可能な場合は、対象問題側だけでなく関連問題側からも同じ比較チャンクへ到達できる双方向の導線にしてください。
- チャンクには、関連する科目・分野・問題番号・肢番号、共通キーワード、結論が分かれる理由と比較軸、本番用の見分け方または合言葉を入れてください。
- 生成済みデータの直接編集だけで終わらせず、再同期後も残るマッピング、補完ユーティリティ、生成元データ等に実装してください。
- チェッカーは、現在の問題と関連問題の双方でチャンクが表示されること、科目名・問題番号・肢番号が正しいこと、既存の解説やチャンクが消えていないことを確認してください。

## 模試3点セット

てらしぃが「模試3点セット」「模試インポート3点セット」などと指示した場合は、次の流れを標準作業として扱ってください。

1. `app/textbook/模試元画像/<模試ID>`、`app/textbook/模試解答/<模試ID>`、`app/textbook/俺の解答用紙/<模試ID>` の3点を確認する。
2. 問題画像、解答画像、本人の解答用紙画像をOCRまたは目視で構造化し、採点できる範囲を明示する。
3. 採点結果は、択一の科目別正解数、記述を除いた点数、記述のみの暫定点、記述込みの点数を出す。記述は採点者裁量があるため、暫定点と根拠を必ず添える。
4. 模試から得た知識は、著作権保護のため問題文や解説文を転載せず、短い正しい知識へ要約して `見て聞いて覚えるモードぷらす` の候補にする。
5. 問題を解くモードのボーナスステージへ入れる場合は、元問題と同一の形式・文章・選択肢にせず、論点だけを抽出した新作問題として作る。
6. 君の教科書には、てらしぃの苦手分野、誤答原因、復習アクション、問題候補、解説、解説図候補をセットで追加する。
7. 実装上は `utils/moshi-complete-input.ts` の3点セット生成関数を優先して使い、必要に応じて `src/tac_learn_content.js`、`src/tac_bonus_questions.js`、`app/textbook/kimi.tsx` へ反映する。
8. 画像や教材を生成した場合は、生成だけで終わらせず、見て聞いて覚える、ボーナスステージ、君の教科書、もっと深掘るのどこに置くかまで判断する。

## 模試の論点インプット

- 模試画像の保存、OCR全文、正解番号、採点結果だけでは「インプット完了」としない。
- 各問題を、模試ID、回、問題番号、科目、分野、論点名、出題の狙い、正しい判断ルール、典型的なひっかけ、根拠、出典、確認状態へ構造化する。
- OCRの文字列をそのまま論点名や学習カードに使わない。問題画像と解答画像を突き合わせ、主語・要件・効果・例外・時期が分かる短い知識へ直す。
- OCRが崩れている問題、正解と解説を照合できない問題は推測で補わず、`needs_review` と不足情報を記録する。
- 1問に複数の独立論点がある場合は、問題単位の要約だけで済ませず、再演習できる論点単位へ分割する。
- 問題文・選択肢・解説の全文を転載しない。出題状況と法律知識を要約し、新作問題は人物・数値・事例・肢を作り直す。
- 各 `confirmed` 論点には、見て聞いて覚える用の短い結論、もっと深掘る用の説明、問題を解くモード用の自作問題・正解・解説を揃える。
- 模試インポート後は、科目・分野別の論点一覧をMarkdownで出力し、「どの分野で何が出たか」に問題番号付きで答えられる状態にする。
- 完了報告前に、必須項目、重複ID、正解範囲、深掘りと自作問題の有無、アプリの結合経路を検証する。

## 模試画像→OCR→アプリ知識インポート（標準パイプライン）

てらしぃが「模試画像を入れて知識化」「OCRしてぷらす／ボーナスへ」「TAC画像から問題を作れ」などと指示した場合は、次を標準作業とする。

### 0. 前提フォルダ

- 問題画像: `app/textbook/模試元画像/<模試ID>/`
- 解答画像: `app/textbook/模試解答/<模試ID>/`
- 本人解答用紙（任意）: `app/textbook/俺の解答用紙/<模試ID>/`
- 画像本体は gitignore 対象でよい。フォルダと `.gitkeep` は残す。Explorer で見えない場合は `explorer.excludeGitIgnore: false` を確認する。
- Downloads に ZIP がある場合は `npm run import:tac-zips`（または同等の展開スクリプト）で所定フォルダへ入れる。

### 1. 文字起こし（OCR）

1. 問題画像と解答画像の両方をOCRする（片方だけでは完了にしない）。
2. 既存手段を優先する: `npm run import:moshi -- <模試ID>`、WinOCR系、または画像を直接読んで構造化する。
3. OCR全文は `tmp/moshi-ocr/<模試ID>/` 等の作業領域に残してよいが、**アプリ本文へ全文転載しない**。
4. ページが斜め・暗い・切れている場合は、無理に全文確定せず `needs_review` にする。

### 2. 論点構造化（topics JSON）

各問を次の形へ落とす（`data/moshi/<examId>-topics.json`）:

- `id`, `questionNumber`, `learnSubject`, `quizSubject`, `quizField`
- `topic`（短い論点名）
- `aim`（出題の狙い・初学者向け）
- `rule`（正しい判断ルール。制度説明＋結論）
- `trap`（典型ひっかけ）
- `memory`（見て聞いて覚える用の1〜2文）
- `deepDive`（もっと深掘る用。結論→理由→暗記）
- `practiceQuestion`（自作の prompt / choices / answer / explanation）
- `sourceTrace`（正解の根拠: 解答画像・MD・突合結果）
- `status`: `confirmed` | `needs_review`

ルール:

- 原文の問題文・肢・解説をコピーしない。人物名・数値・事例・肢の言い回しを作り直す。
- 1問に独立論点が複数あるときは、カード／ボーナスを分割する（例: 裁判員の人数／評決／欠格）。
- 正解番号が取れない問は `needs_review`。推測で `confirmed` にしない。

### 3. 初学者向け品質ゲート（必須）

実装を全問展開する前に、**代表3問をてらしぃへ提示**し、次を満たすか確認する。

1. 問題文を読んだだけで、何の制度の話か分かるか。
2. 正解肢が「制度の説明＋結論」になっており、スローガンや記号の羅列だけになっていないか。
3. 誤答肢が「常に／一切／主語無視」など、初学者が実際にやりがちな取り違えか。
4. 解説が1〜3文で、なぜ正解かを言えているか。

品質が足りない場合は全問生成を止め、文言を直してから再提示する。

### 4. アプリへの接続

1. 生成スクリプトで learn / bonus を出力する（例: `npm run build:moshi-topics` / `build:tac-topics` / `build:tac2-topics`）。
2. `src/learnExports.js` に learn を結合する（`source` に `TAC` / `模試` 等を含め、ぷらすフィルタに乗せる）。
3. `src/bonus_questions.js` に bonus を結合する（`isBonus: true`、`choices` / `answer` / `explain` 必須）。
4. 各カードに `deepdive`（もっと深掘る）を空にしない。
5. 必要なら `npm run build:chat-md-index` で④質問する用索引を更新する。
6. 件数検証: 対象模試の learn 件数、bonus 件数、空 deepdive 数、ぷらす抽出ヒットを確認する。

### 5. 完了条件

- 見て聞いて覚えるモードぷらすで、対象模試のカードが読める。
- 問題を解くモードのボーナスステージで、自作問題が解ける。
- もっと深掘るが空でない。
- 著作権上、元模試の全文転載がない。
- 未突合・要確認が残る場合は、件数と不足情報をてらしぃへ報告する。

## 質問モード：紛らわしい比較ガイドの自作フロー

てらしぃが「紛らわしい論点を自分で質問して答えを作れ」「比較を深掘りして質問モードを強くして」などと指示した場合、または科目ごとに質問モード強化を進める場合は、次を標準とする。

### 目的

- 読み・数字・制度名が似ていて取り違えやすい対を、**自問自答の比較ガイド**として質問モードに載せる。
- 口語（「〜ってなん？」「〜の違いは？」）でも検索に当たるようにする。

### 実施手順

1. 対象科目の既存知識（見て聞いて覚える、まとめMD、quiz/learn MD）を読み、**紛らわしい対**をエージェント側で列挙する（てらしぃの例: 実定法と実体法）。
2. 各対について、仮想質問「AとBの違いは？」を立て、結論・対比表・ひっかけ・暗記を短いガイドにする。
3. 配置先:
   - 科目の単体ガイド: `utils/chatTopicBriefs*.ts`（例: `chatTopicBriefsKisoHougaku.ts`、`chatTopicBriefsGyoseiSoron.ts`）
   - **比較専用**: `utils/chatTopicBriefsKisoComparisons.ts`（他科目も同型で追加可）
   - **模試由来**: `utils/chatTopicBriefsKisoMoshi.ts`（TAC・合格革命など。learn/bonus にある論点を質問モードでも口語ヒットさせる）
   - **行政法総論・判例クラスタ**: `utils/chatTopicBriefsGyoseiSoron.ts`（距離規制・処分性・強制執行・公務員判例など。類似判例の提示を必須）
   - **行政法総論・X/ネット口語FAQ**: `utils/chatTopicBriefsGyoseiSoronNet.ts`（「わからん」「ってなに」系。詳細手順は後述「X・ネット口語FAQの取り込み」）
   - **行政手続法**: `utils/chatTopicBriefsGyoseiTetsuzuki.ts`（申請／届出、審査基準、聴聞・弁明、意見公募など。条文芯＋模試の多角Q）
   - **行政不服審査法**: `utils/chatTopicBriefsGyoseiFufuku.ts`（審査請求／再調査／再審査、審理員、執行停止、教示、裁決など）
   - **手続系横断比較**: `utils/chatTopicBriefsGyoseiComparisons.ts`（審理員↔主宰者、努力義務並列、執行停止・行服↔行訴。「違いをまとめて」用）
   - **行政事件訴訟法**: `utils/chatTopicBriefsGyoseiGyosho.ts`（取消・義務付け・差止め・無効確認・出訴期間・判決効。行服法との横断比較含む）
   - **憲法**: `utils/chatTopicBriefsKenpou.ts`（二重の基準・私人間効力・政教分離・職業選択・統治機構。行政法との聞き分け含む）
   - **国家賠償法**: `utils/chatTopicBriefsKokubai.ts`（1条／2条、職務行為基準、規制権限不行使、営造物判例クラスタ）
4. `utils/chatKnowledgeSearch.ts` の `CHAT_TOPIC_BRIEFS`（または同等の結合箇所）に接続する。トリガーには「AとB」「BとA」「A B」を含める。
5. 模試論点を質問モードへ足すときは、既に `見て聞いて覚える`／ボーナスへ入っていても、**比較・口語トリガー付きガイド**を別途足す（learn カード全文に頼らない）。
6. 判例ガイドでは、重要ワード・ざっくり流れ・**類似判例／紛らわしい別論点**（例: 薬局距離↔小売市場距離、病院「距離」↔開設中止勧告の処分性）を必ず入れる。
7. **手続法・不服審査法**では、条文の区別（義務／努力義務、誰が誰に、期間、例外）を表にし、過去問・模試でよくある「どちら？」「いつ？」「誰？」の角度を多角Qに入れる。
8. 動作確認: `npm run check:chat-briefs`（トリガー照合）。必要なら代表質問をアプリの⑤質問するでも確認する。
9. 完了報告では、**作った比較一覧（仮想質問→ガイド題名）**をてらしぃへ提示する。

### 品質

- 既存教材・条文と矛盾させない。未確認は断定しない。
- 原文の問題文・解説の全文転載はしない。
- 比較軸が違うもの（例: 実定法↔自然法 と 実体法↔手続法）は「別軸」と明示する。

### キーワードに頭を取られる論点の多角質問（必須）

てらしぃが質問モードを「サクサク答えられるように」強化するよう指示した場合、または模試・基礎論点を質問モードへ載せる場合は、**1論点1質問で終わらせない**。

受験生は目立つキーワード（例: 「くじ引き」「20日」「140万円」）に意識を取られ、試験が本当に聞いている芯（例: **誰が**選任されるか／**何の制度か**／**反対概念は何か**）が抜けやすい。

#### やり方

1. まず芯を一文で書く（例: くじ＝一般市民。典型は検察審査員と裁判員）。
2. 同じ芯を、次のような**別角度の仮想質問**に作り直す（最低5方向）。
   - **誰か**（主体）
   - **何をするか**（役割・効果）
   - **いくつ／いつ**（人数・期間・数字）
   - **どう選ぶか**（手続・キーワード側）
   - **誰ではないか**（除外・ひっかけ）
   - **似た制度との違い**（検審11 vs 裁判員9 など）
   - **口語**（「ってなに」「だれ？」）
3. ガイド本文に「キーワードに釣られない」注意と、多角Qを短く並べる。
4. トリガー語はキーワード側（くじ引き・抽選）と芯側（検察審査員・裁判員・誰）の**両方**を入れる。
5. 検索トークンにも、キーワードから芯語への展開を足す（例: 「くじ」→ 検察審査員・裁判員・11人）。
6. `npm run check:chat-briefs` に、多角質問の代表クエリを追加して照合する。

#### 完了条件

- キーワードだけの質問でも、芯（誰／何／いくつ）が答えに出るガイドがヒットする。
- てらしぃへの報告で、**作り直した多角質問一覧**を提示する。

## 質問モード：X・ネット口語FAQの取り込み（行政法総論ほか）

てらしぃが「Xのつぶやきやネットの質問を拾って答えられるようにして」と指示した場合、または科目の質問モードを受験生の口語レベルまで引き上げる場合は、次を標準とする。

### 目的

- 受験生が X・ブログ・note・知恵袋などで実際に投げる口語（「処分性わからん」「許可と認可なにが違う」「条例に処分性あるの？」）に、⑤質問するが即答できる状態にする。
- 判例名・条文番号だけのガイドにせず、**つぶやき口調の仮想質問 → 結論 → 対比表 → 類似論点 → 暗記**まで入れる。

### 実施手順

1. **収集**（API全件スクレイプはしない。公開検索・学習記事・模試論点・既存learnから「よくある混乱」を列挙する）
   - 検索例: `行政書士 処分性 わからない`、`許可 認可 特許 違い`、`法律の留保 侵害留保`、`条例 処分性`
   - Xは公開で見つかる受験つぶやき・まとめ記事の**論点パターン**を拾う（個別アカウントの私信・有料全文は使わない）
2. **仮想質問化**: 収集した言い回しを、口語トリガー付きのFAQにする（「ってなに」「わからん」「むず」「あるの？」）
3. **配置先**
   - 判例・模試クラスタ: `utils/chatTopicBriefsGyoseiSoron.ts`
   - **X・ネット口語FAQ**: `utils/chatTopicBriefsGyoseiSoronNet.ts`
4. `utils/chatKnowledgeSearch.ts` に briefs・key phrases・aliases を結合。必要ならトークン展開（例: 処分性→取消訴訟・法律上の利益）
5. `npm run check:chat-briefs` に代表口語クエリを追加して照合
6. 完了報告では、**拾った口語質問一覧 → ガイド題名**をてらしぃへ提示する

### 品質・禁止

- 原文の問題文・有料記事・他人のつぶやき全文を転載しない。論点パターンと正しい知識だけ自作する
- 未確認判例の結論は断定しない。既存learn・ナレッジと矛盾させない
- 「全部のXを網羅」は不可能なので、**頻出混乱クラスタを飽和させる**ことを完了条件にする。追加のつぶやき例があれば追撃で足す

### 行政法総論の完了イメージ（現時点の柱）

- 距離規制・処分性判例・強制執行・公務員判例（`GyoseiSoron`）
- 法律による行政の原理、許可認可特許、処分性／原告適格口語、条例・建築確認・用途地域、国賠と損失補償など（`GyoseiSoronNet`）

### 行政手続法・行政不服審査法（条文多角）

てらしぃが行手法／行服法を「条文を詳しく・あらゆる角度で」と指示した場合:

1. `見て聞いて覚える`（行政手続法・行政不服審査法）と模試 topics（TAC等）から、義務／努力義務・誰が誰に・期間・例外のひっかけを列挙する
2. 配置: `utils/chatTopicBriefsGyoseiTetsuzuki.ts` / `utils/chatTopicBriefsGyoseiFufuku.ts`
3. **横断比較**は `utils/chatTopicBriefsGyoseiComparisons.ts`（審理員↔主宰者、努力義務並列、執行停止・行服↔行訴 など）
4. 各ガイドに「違い」「いつ」「誰」「例外」「模試定番ひっかけ」の多角Qを入れる
5. 行手法と行服法の境界（処分等の求め／指導中止の求めは行手法、不服の中身は行服法）を明示する
6. 「違いをまとめて」と言われたら比較ガイドを優先ヒットさせる（`topicBriefsForQuery` の比較ブースト）。⑤質問するのサジェストチップにも代表比較を置く
7. `npm run check:chat-briefs` で口語クエリを照合

### 行政事件訴訟法（条文多角）

てらしぃが行訴法を進めるよう指示した場合:

1. 配置: `utils/chatTopicBriefsGyoseiGyosho.ts`
2. 必須クラスタ: 訴訟類型、取消訴訟の入口順番、出訴期間（行服3か月との対比）、執行停止、無効確認の補充性、義務付け（申請型／非申請型）・差止め・仮の救済、事情判決、判決効、国賠との関係
3. 「執行停止の違いをまとめて」「行服法と行訴法の違いをまとめて」で横断比較が出ること
4. `npm run check:chat-briefs` で照合

### 憲法（条文・判例多角）

てらしぃが憲法の質問モード強化を指示した場合:

1. 配置: `utils/chatTopicBriefsKenpou.ts`（枠組み）＋ `KenpouHanrei.ts` / `KenpouHanrei2.ts`（個別判例）＋ `KenpouDeep.ts`（判断枠組みの深掘り）
2. 必須クラスタ: 二重の基準、公共の福祉、私人間効力、外国人（マクリーン）、検閲／事前抑制、職業選択（目的二分・薬局距離）、政教分離（目的効果）、生存権（朝日・堀木）、公務員人権、司法権の限界、国会・内閣・地方自治
3. 薄い／別名しかない定番判例は Hanrei / Hanrei2 に、判旨要約（結論→ひっかけ→暗記）を足す。第1波例: 昭和女子大・日産・泉佐野・合祀・全農林系・投票価値。第2波例: 北方ジャーナル・よど号・税関・博多駅・尊属殺・津／愛媛／空知太・森林法・苫米地・砂川・警察予備隊・旭川学テ・前科照会・謝罪広告
4. **深い出題対策**は `KenpouDeep.ts`: 目的効果↔総合考慮、政教判例聞き分け、規制目的二分・明白性・LRA、検閲4要素・事前抑制例外、平等結論表、司法権の限界棚、答案型メニュー
5. 行政法と論点が交差するもの（薬局距離・公務員政治活動・長沼）は「憲法側／行政法側」の聞き分けを明示する
6. `npm run check:chat-briefs` で照合

### 国家賠償法（判例・枠組み）

てらしぃが国賠の質問モード強化を指示した場合:

1. 配置: `utils/chatTopicBriefsKokubai.ts`
2. 必須クラスタ: 1条↔2条、職務行為基準、公務員個人責任／求償、パトカー追跡、規制権限不行使（筑豊・クロロキン・食品）、建築確認、立法不作為、高知落石／大東／多摩川（改修後の河川管理）／赤色灯／故障車放置／テニス審判台（異常用法）／空港騒音、道路↔河川比較、混同防止ひっかけ、国賠↔損失補償、取消前置不要
3. learn／過去問で事件名が薄い定番は `src/kokubai_learn_content.js` にも見て聞いて覚えるカード＋deepdiveを足し、`learnExports.js` で結合する。原文転載なし
4. `npm run check:chat-briefs` で照合

### YouTube・外部動画からの取り込み

てらしぃが「中チャンネル」等の YouTube を知識源にするよう指示した場合:

1. **動画の音声・字幕を自動で丸ごと取り込むことはしない**（規約・著作権・品質）。全文転載禁止
2. できること: 公開されているタイトル／説明／てらしぃが貼った要約・メモ・字幕抜粋から、**論点パターンだけ**を自作ガイド化する
3. チャンネルURLまたは「この動画の論点リスト」をてらしぃから受け取ったら、`GyoseiComparisons` / `Tetsuzuki` / `Fufuku` / `Gyosho` に追記する
4. アプリ内に既にある深掘り（例: 努力義務シリーズ）と矛盾させない。矛盾候補はてらしぃ確認

## 作業ルール

- まず `git status --short` を確認し、既存の未コミット変更を壊さないでください。
- ユーザーが触っている可能性のある変更は戻さないでください。
- 無関係なリファクタ、整形、ファイル移動、生成物更新は避けてください。
- 大きなファイルや生成済みファイルを変更する場合は、変更理由と検証方法を明確にしてください。
- 破壊的な操作、依存関係の更新、大量生成、外部API呼び出しは必要性を確認してから行ってください。
- `.env`, `credentials/`, APIキー、認証情報、個人データを出力・コミットしないでください。

## 検証方針

- 通常のコード変更後は、可能な範囲で `npm run lint` を実行してください。
- クイズ・学習データ同期に関わる変更では、関連する同期・生成・検証スクリプトを必要最小限で実行してください。
- UI変更では `npm run web` または Expo 開発サーバーで該当画面を確認してください。
- 実行できなかった検証がある場合は、理由と残るリスクを報告してください。

## エージェントの振る舞い

- 日本語の依頼には日本語で応答してください。
- 変更前に周辺コードを読み、既存パターンに寄せて実装してください。
- 迷った場合は、小さく安全な変更を選び、根拠を短く説明してください。
- 依頼が実装作業なら、提案だけで止まらず、可能な限り実装と確認まで進めてください。
- 最終報告では、変更したファイル、実行した確認、未確認事項を簡潔に伝えてください。
- 主のことを、”てらしぃ”と呼ぶこと
